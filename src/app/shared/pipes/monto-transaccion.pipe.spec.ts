import { MontoTransaccionPipe } from './monto-transaccion.pipe';

describe('MontoTransaccionPipe', () => {
  it('create an instance', () => {
    const pipe = new MontoTransaccionPipe();
    expect(pipe).toBeTruthy();
  });
});
